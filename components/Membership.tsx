'use client';

import { motion } from 'framer-motion';
import { fadeIn } from '@/constants/animations';

const Membership = () => {
  return (
    <section className="h-screen bg-pink-300" id="prices">
      <div>
        <h2>Membership Plans</h2>
      </div>
    </section>
  );
};

export default Membership;
