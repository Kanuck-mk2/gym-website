const CustomButton = ({
  text,
  containerStyles,
}: {
  text: string;
  containerStyles: string;
}) => {
  return <button><span>
    </span>
    <span>{text}</span></button>;
};

export default CustomButton;
